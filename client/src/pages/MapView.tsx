import Dashboard from "./Dashboard";

// Re-using the main dashboard view as the map view for this MVP
// since the dashboard is already map-centric.
export default function MapView() {
  return <Dashboard />;
}
